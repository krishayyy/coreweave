__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.*
data/tiles/
# Intermediate runs stay out; the records the README cites are committed, so a
# clone can check the numbers instead of taking them on trust.
runs/*
!runs/README.md
!runs/experiment_headline.json
!runs/time_curve.json
!runs/learning_curve.json
!runs/time_curve.png
!runs/learning_curve.png
node_modules/
dist/
.DS_Store
data/llm_cache/
data/jev_cache/
data/imagery/
